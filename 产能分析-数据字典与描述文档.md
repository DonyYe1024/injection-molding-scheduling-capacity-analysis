# 《最新排程产能分析-公式模板》数据字典与描述文档

> 用途：本文档完整描述该 Excel 工作簿（15 个工作表）的结构、表头、公式逻辑、图表组合与表间关系，作为「HTML 个人工作台」开发的准确依据。
> 数据版本：更新日期 2026-08-28（原始数据1 更新日期）。

---

## 一、工作簿总览（15 张表）

| # | 工作表 | 类型 | 维度 | 用途 |
|---|--------|------|------|------|
| 1 | 分析1-模具产能分析 | 分析 | 156行×BF列 | 按模具+周，汇总扣库存后待生产数量与工时，对比产能 |
| 2 | 分析2-机台产能分析 | 分析 | 96行×AY列 | 按机台组+周，实单/产能/预算工时对比 |
| 3 | 分析3-实单+预算待生产前十占比分析 | 分析 | 209行×AB列 | 外壳/配件待生产前十名占比 + GAP 目标缺口 |
| 4 | 分析4-原料需求-库存水位-入库产值统计 | 分析 | 72行×43列 | 原料需求 + 注塑外壳库存水位 + 入库汇总 + 年度汇总（已由旧「分析4-注塑半成品原料库存水位」更名） |
| 5 | 注塑模具产能问题GAP（王力预算） | 分析 | 166行×BD列 | 模具产能缺口（GAP）与上班天数 |
| 6 | 原始数据1（工单备料明细） | 源数据 | 16596行×AA列 | 核心源表：工单明细+派生计算列 |
| 7 | 原始数据2(最新排程上线日期) | 源数据 | 7861行 | 订单号→上线日期映射 |
| 8 | 原始数据3(DKL最新预算数量-工时) | 源数据 | 19262行×DL列 | 预算数量+工时，按产品×周 |
| 9 | 原始数据4(产能参数) | 源数据 | 943行×V列 | 模具/机台产能参数（机台、穴数、周期、毛重） |
| 10 | 原始数据5（外壳产值单价） | 源数据 | 56087行×R列 | 入库产值（数量、单价、金额） |
| 11 | 原始数据6(DKL最新预算估值-金额) | 源数据 | 13665行×DL列 | 预算金额，按产品型号×周 |
| 12 | 原始数据7（原料估算） | 源数据 | 19810行×DL列 | 原料需求，按机台组×周 |
| 13 | 原始数据9（注塑半成品原料库存水位） | 源数据 | 1445行×21列 | 原料库存水位明细（按品名分类：塑胶外壳类/通风系统类/调整器类…） |
| 14 | BOM计算 | 源数据 | 14733行×P列 | 物料清单（主件→元件→原料） |
| 15 | 备用报表 | 辅助 | 150行×DB列 | 周排产汇总（实单/预算/产能/排产量） |

---

## 二、表间数据流与引用关系

```
原始数据2(上线日期) ──VLOOKUP──┐
原始数据4(产能参数) ──VLOOKUP──┼──► 原始数据1(工单备料明细) ──SUMIFS──► 分析1(模具产能)
BOM计算 ───────────VLOOKUP────┘           │
                                          ├─SUMIFS/汇总──► 分析2(机台产能)  ◄── 原始数据3(预算工时)
                                          │
原始数据5(产值单价) ──VLOOKUP──► 原始数据3/6/7(预算/金额/原料估算)
                                          │
原始数据7(原料估算) + 原始数据9(原料库存) ──► 分析4(原料需求/库存水位/入库产值)
                                          │
原始数据1 + 原始数据3 ──► 分析3(前十占比/GAP)
备用报表(上班天数/排产) ──HLOOKUP──► 注塑模具产能问题GAP
```

**核心链**：原始数据1 是枢纽，其 J~W 列通过 VLOOKUP 关联原始数据2/4/BOM 自动计算，产出「扣库存后待生产」「工时」「机台」「周数」等，供分析1/2/3 汇总。

---

## 三、核心业务规则（公式口径）

### 3.1 原始数据1 的派生列公式（J~W 列）
表头第 3 行、数据第 4 行起。基础输入列为 A~I。

| 列 | 名称 | 公式（功能 → 指向） |
|----|------|----------------------|
| J | 订单生产工时 | `ROUNDUP(S×L/3600/穴数,2)`；穴数取 R 值 `"1*16"` 中 `*` 后数字 |
| K | 待生产累计 | `SUMIF(按发料料号C 累计未发数量H)`，逐行 |
| L | 扣库存后待生产 | 三段 IF：`I>K→0`；`料号变→H`；否则 `OR(下段累计≥K, 上L≠0)→H` 否则 `K-I` |
| M | 订单上线日期 | `VLOOKUP(批号B → 原始数据2 A:B 第2列)` |
| N | 品种 | `VLOOKUP(料号C → 原始数据4 A:E 第5列)` |
| O | 注塑生产日期 | `M - 7天` |
| P | 周数 | `YEAR(O)&"WK"&WEEKNUM(O)` |
| Q | 使用机台 | `VLOOKUP(料号C → 原始数据4 A:H 第8列)` |
| R | 穴数 | `VLOOKUP(料号C → 原始数据4 A:K 第11列)`，格式 `1*1`（模具数*穴数） |
| S | 周期 | `VLOOKUP(料号C → 原始数据4 A:M 第13列)`，单位秒 |
| T | 毛重 | `VLOOKUP(料号C → 原始数据4 A:O 第15列)`，单位克 |
| U | 原料代码 | `VLOOKUP(料号C → BOM计算 C:D 第2列)` |
| V | 原料名称 | `VLOOKUP(料号C → BOM计算 C:K 第9列)` |
| W | 原料预算(KG) | `L × T / 1000` |

### 3.2 机台分组（分析2 用）
| 机台组 | 包含 | 产能(6天=22h×6天) |
|--------|------|-------------------|
| 400T至550T注塑外壳 | 400T/450T/480T/550T | 10台→1320h，11台→1452h |
| 230T+260T+280T配件 | 230T/260T/280T | 3台→396h，4台→528h |
| 150T | 150T | 1台→132h(6天)/154h(7天) |
| 90T | 90T | 1台→132h，2台→264h |
| 150T-280T合并 | 150T+230T+260T+280T | 4台→528h，5台→660h |

**机台具体化映射（改善5）**：原始数据4 的 H列【机台】已从笼统表示改为具体吨位，抽取脚本 `_extract_mappings.py` 的 `map_machine()` 将其映射回网页笼统分组：
- `85T`、`90T` → `90T`
- `150T` → `150T`
- `230T`、`260T` → `230T-280T`
- `400T`、`450T`、`480T`、`550T` → `400T-550T`
- `0`/空/`#N/A` → 非注塑件，跳过

### 3.3 周维度
格式 `2026WKxx`，从 `WK32`（2026）延续到 `2027WK4`；周数用 Excel `WEEKNUM`（周日为一周首日，1/1 所在周为第 1 周）。

### 3.4 模具编号提取
从规格（如 `602204458/DH010S/外壳…`）用正则 `DH\d{3}[A-Z]?` 提取，得到 `DH010S`、`DH010M` 等模具号。

---

## 四、逐表详细描述

### 表1 分析1-模具产能分析
- **布局**：行 = 模具（每个模具 2 行：白色款 + 黑色款），列 = 周（从 `原始数据3` 的周表头引用）。
- **数量公式**：`=SUMIFS(原始数据1!$L:$L, 原始数据1!$P:$P, 本列周, 原始数据1!$C:$C, $A本行料号)` —— 按「周 + 料号」汇总扣库存后待生产数量。
- **子区块**（每个模具后）：`XX模具产能分析（已扣库存）`，含三行：
  - 实单工时 = `数量 × 周期(60秒) / 3600`
  - 6天工时 = `1套模具×22h×6天 = 132`，2套=264，3套=396
  - 预算工时 = `预算数量 × 60 / 3600`
- **图表**：4 个堆叠柱状图，系列 = 「8/7组装主排程待生产实单工时」+「8/8王力预算工时」。
- **模具清单**：DH010S / DH010M / DH010L / DH010XS / DH001-S / DH025 系列等。

### 表2 分析2-机台产能分析
- **布局**：行 = 周（WK32→2027WK4），列 = 5 个机台组区块，每组 4 列（实单、6天产能、7天产能、预算）。
- **系列含义**：
  - 实单 = 未发料实单待生产工时(8/7更新，已扣库存)
  - 预算 = DKL最新预算生产工时(王力8/8运算)
- **图表**：5 个堆叠柱状图（每组 1 个），系列 = 实单 + 预算（堆叠）。
- **数据特征**：实单集中在 WK33~WK48，预算从 WK36 起、WK39 后大幅上升（预算高峰）。

### 表3 分析3-实单+预算待生产前十占比分析
- **KPI 区**：外壳实单+预算、外壳金额、配件金额、金额合计、本月目标值、GAP、计划提拉金额（含提拉外壳/配件数量）。
- **前十榜单**：左侧「外壳」、右侧「配件」。外壳列 = A品名 / B实单+预算PCS / C金额 / D实单数量 / E预算数量；配件列 = F品名 / G实单+预算PCS / H金额 / I实单数量 / J预算数量（配件新增 I/J 列，与外壳字段对齐：name/pcs/amount/realQty/budgetQty）。R16 为表头，R17 起为数据。配件榜首 塑料铆钉（实单 0 / 预算 89918 PCS）。
- **图表**：2 个饼图 ——「外壳实单+预算(PCS)」「配件实单+预算(PCS)」。

### 表4 分析4-原料需求-库存水位-入库产值统计（原「分析4-注塑半成品原料库存水位」已更名）
- **原料需求子表**（A4:AQ4 表头，A5:A32=28 种原料）：A=使用产品 B=物料代码 C=物料名称 D=说明 E=合计(2026WK37-2027WK21实单+预算原料预算) F~AQ=38 周（2026WK37~2027WK21）。
- **注塑外壳库存水位子表**（D36:D42，E=数量PCS F=金额RMB G=占比）：180天以上=1114 / 120天=16792 / 90天=10491 / 60天=19726 / 小于60=82738 / 合计=130861。
- **KPI目标子表**（C47:D49）：本月目标金额 3000000、计算天数 26、日均目标值 115384.6。
- **入库汇总子表**（A51:A59，C52=月计，D52~AG52=2026-09-01~09-30 每日日期）：生产数量组（外壳/配件/合计）、入库金额组（外壳RMB/配件RMB/合计）、达成率(Rate)。
- **年度汇总子表**（A61:A72，C62=2026年度，D62~O62=1月~12月）：生产数量组、入库金额组、目标KPI(D71=2800000…)、达成率(Rate)。
- **用途**：原料需求水位（对照原始数据9 库存）+ 外壳库存水位饼图 + 入库/年度产值统计。

### 表5 注塑模具产能问题GAP（王力预算）
- **行**：上班天数（HLOOKUP 备用报表）、各周 GAP 计算。
- **列**：周分布（WK34→WK31），周日期用 `TEXT(DATE(...)+(RIGHT(...)))` 从周号反推。
- **用途**：模具产能缺口 = 需求 vs 可供产能。

### 表6 原始数据1（工单备料明细）—— 核心
- **表头（27列）**：A验货日期 B预计入库批号 C发料料号 D品名 E规格 F应发数量 G已发数量 H未发数量 I可用库存量 J订单生产工时 K待生产累计 L扣库存后待生产 M订单上线日期 N品种 O注塑生产日期 P周数 Q使用机台 R穴数 S周期 T毛重 U原料代码 V原料名称 W原料预算(KG) X外壳类型 Y/Z/AA备注。
- **公式**：见 3.1。
- **关键列**：C=料号(关联键)、H=未发、I=库存、L=扣库存后待生产、P=周、Q=机台、S=周期、R=穴数。
- **数据规模**：16,596 行。

### 表7 原始数据2(最新排程上线日期)
- **列**：訂單號碼(A) | 上线日期(B) | 验货订单(C) | 验货日期(D)；右侧 T/U 列为另一组（订单号/验货日期）。
- **用途**：订单号→上线日期映射（原始数据1 的 M 列 VLOOKUP 来源）。

### 表8 原始数据3(DKL最新预算数量-工时)
- **结构**：周表头（L/M/N… 成对：数量列+工时列）；机台汇总行（SUMIFS 按机台）；明细行（VLOOKUP 产能参数取周期/穴数/品种/机台，VLOOKUP 原始数据5 取单价，ROUNDUP 算工时）。
- **用途**：预算数量与工时（王力运算），供分析1/2 的预算线。

### 表9 原始数据4(产能参数)
- **表头**：A新物料代码 B产品品类代码 C类别 D规格名称 E备注/简称 F后工序类别 G旧模具编号 H机台 I模具数 J模具尺寸 K穴数 L型号1 M周期(秒) N参考净重(g) O参考毛重(g) P参考产值单价 R5.0料号 S5.0规格 T王力预算5.0料号 U王力预算item号 V备注。
- **注**：最新版新增 P=参考产值单价 列；H列【机台】已从笼统表示改为具体吨位（当前唯一值：480T/150T/260T/90T/400T/230T/550T/450T/85T/0），抽取时经 `map_machine()` 映射回笼统分组（见 3.2）。
- **用途**：料号→机台/穴数/周期/毛重 的核心映射（原始数据1 N/Q/R/S/T 列来源）。

### 表10 原始数据5（外壳产值单价）
- **表头**：A入库日期 B仓库 C部门 D申请人 E工单号 F料号 G品名 H规格 I批号 J入库数量 K工单数量 L已入库总数 M未入库总数 N单价 O金额(CNY) P品种 Q月份。
- **用途**：入库产值记录（2022 至今，56,087 行）。

### 表11 原始数据6(DKL最新预算估值-金额)
- **结构**：行 = 产品型号（DH001S/DH001XS/DH010L…），列 = 周；SUMIFS 按型号汇总金额。
- **用途**：预算金额（万元级），按型号×周。

### 表12 原始数据7（原料估算）
- **结构**：行 = 机台组（400T-550T/90T/150T/230T-260T…），列 = 周；SUMIFS 按机台汇总原料需求。
- **用途**：原料需求估算（KG），按机台×周，供分析4。

### 表13 原始数据9（注塑半成品原料库存水位）
- **表头（21 列）**：A=据点 B=料号 C=品名(分类，如「塑胶外壳类」484 行/通风系统类/调整器类…) D=规格 E=库位编号 F=库位名称 G=储位 H=批号 I=库存单位 J=库存量 K=在拣量 L=扣除在拣量库存 M=备置数量 N=库存可用否 O=最后异动日期 P=第一次入库日期 Q=呆滞日期 R=单价 S=金额 T=库领 U=库存时间(如「180天以上」)。
- **用途**：原料实时库存明细。旧版「分析4 的 141 件外壳库存明细」来源已消失，现看板「外壳库存水位」表改从本表抽取：筛选 C列品名=「塑胶外壳类」、按 B列料号去重（保留 code/name/spec），库存总数 = 该子集 J列库存量求和。

### 表14 BOM计算
- **表头**：料号 | ITEM | 主件 | 元件 | 组成用量 | 底数 | 单位 | 最终用量 | 阶层 | 元件品名 | 元件规格 | 周期 | 类别 | 品名 | 使用机台 | 单价。
- **关联**：主件(C)→元件(D)=原料代码，主件(C)→元件规格(K)=原料名称（原始数据1 U/V 列来源）。

### 表15 备用报表
- **行（类别）**：实单数量(MTO)、预算数量(MTO)、客户需求(实单MTS+…)、注塑可供产能、需要排产数量。
- **列**：周（WK34→WK32），附月份。
- **用途**：排产汇总，供 GAP 表取上班天数。

---

## 五、关键指标（KPI）定义

| 指标 | 定义 | 数据来源 |
|------|------|----------|
| 扣库存后待生产(L) | 未发数量扣减可用库存后的净待生产量 | 原始数据1 L列 |
| 订单生产工时(J) | 周期×扣库存量÷3600÷穴数 | 原始数据1 J列 |
| 实单工时 | 按机台/模具/周汇总的 J 列 | 分析1/2 |
| 预算工时 | DKL 王力预算的工时 | 原始数据3 → 分析1/2 |
| 产能工时 | 台数×22h×天数（6/7天） | 分析2 固定值 |
| 待生产前十占比 | 外壳/配件 实单+预算 PCS 前十 | 分析3 |
| GAP | 目标产值 − 实单+预算金额 | 分析3 |
| 原料需求(KG) | L×毛重÷1000 或原始数据7 | 分析4 |
| KPI日均目标值 | 计划入库产值KPI ÷ 计划开机天数（保留 1 位小数，万RMB） | KPI 卡片（第 5 张计算展示卡） |

---

## 六、HTML 个人工作台落地建议

1. **机台产能模块**：还原分析2 —— 5 组机台 Tab，每组「实单+预算」堆叠柱状图（周为 X 轴），产能线可加参考线。
2. **模具产能模块**：还原分析1 —— 模具 TOP 榜 + 模具×周 实单/预算 堆叠图。
3. **待生产前十模块**：还原分析3 —— 外壳/配件双饼图 + KPI 卡片（目标、GAP、提拉金额、KPI日均目标值）。
4. **原料需求模块**：还原分析4 原料需求子表 —— 原料周需求表（首列「使用产品」超长按 / 换行）+ 外壳库存水位（饼图 + D36:F42 明细表）。
5. **入库/年度汇总模块**：还原分析4 入库汇总 + 年度汇总子表 —— 横向滑动表格（每日/每月 数量、金额保留 0 位小数、达成率<100% 红字）。
6. **数据上传**：拖拽「原始数据1」自动按 3.1 公式计算 J~W 列（需内嵌原始数据2/4/BOM 三张映射表）。
7. **产值模块**：还原原始数据5 —— 月度入库金额/数量趋势 + 品类占比。
8. **顶部仪表盘**：月度注塑机台负荷仪表盘（月度负荷理论最大值（万PCS）= 1500PCS × 11 台 × 开机天数；负荷% = 待生产外壳数量 ÷ 理论最大值）+ 月历（含农历、月份切换）。
9. **技术栈**：单文件 HTML + ECharts + SheetJS，数据内嵌（file:// 双击即开），上传重算用 O(n) 逐行扫描。

---

*本文档由《最新排程产能分析-公式模板-最新.xlsm》（2026-08-10 版）完整解析生成。*
